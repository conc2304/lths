import { Typography, Box } from '@mui/material';

const DesignSystem = (): JSX.Element => {
  return (
    <Box title="MMS Design System" width={'100%'} padding={5}>
      <Typography variant="h1" textAlign={'center'}>
        MMS Design System!
      </Typography>
    </Box>
  );
};

export default DesignSystem;
