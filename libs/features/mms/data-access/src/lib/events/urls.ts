export const getEventsUrl = () => {
  return `/mms/events`;
};

export const createEventUrl = () => {
  return `/mms/events`;
};

export const updateEventUrl = (id: string | number) => {
  return `/mms/events/${id}`;
};
