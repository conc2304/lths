import { FormChildren } from '@lths/shared/ui-elements';

import { withFilterFormContext } from '../../context/withContext';

export const ConnectedFormChildren = withFilterFormContext(FormChildren);
